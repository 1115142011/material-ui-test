import { makeStyles } from '@material-ui/core/styles'

const baseInputStyle = makeStyles({
  root: {
    background: '#fafafa',
    height: 45,
    border: 0,
    paddingLeft: 8,
  },
})

export { baseInputStyle }
